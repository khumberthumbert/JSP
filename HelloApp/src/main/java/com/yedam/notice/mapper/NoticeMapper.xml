<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yedam.notice.mapper.NoticeMapper">
	<select id="getEmp" parameterType="int" resultType="employee">
		select * from employees where employee_id = #{empId} <!-- #{파라메타} 한권 조회 
			한다는 뜻. -->
	</select>
	<select id="empList" resultType="employee"> <!-- id를 메소드 이름으로 생각. resultType : 반환되는 타입 지정. -->
		select * from employees order by employee_id
	</select>
	<delete id="delEmp" parameterType="int">
		delete from employees where
		employee_id = #{id}
	</delete>
	<insert id="addEmp" parameterType="employee">
		insert into
		employees(employee_id,last_name,email,job_id,hire_date)
		values(#{employeeId},#{lastName},#{email},#{jobId},sysdate)
	</insert>

	<!-- 공지사항관련. -->
	<select id="noticeList" resultType="notice">
		select * from tbl_notice order by 1 desc
	</select>

	<insert id="insertNotice" parameterType="notice">
		insert into tbl_notice (notice_id, notice_writer, notice_title,
		notice_subject)
		values(notice_seq.nextval, #{noticeWriter}, #{noticeTitle}, #{noticeSubject})
	</insert>
	
	<update id="updateNotice" parameterType="notice">
		update tbl_notice
		set notice_title=#{noticeTitle},
			notice_subject=#{noticeSubject}
		where notice_id =#{noticeId}
	</update>

	<delete id="deleteNotice" parameterType="int">
		delete from tbl_notice where notice_id=#{noticeId}
	</delete>
	
	<select id="searchNotice" resultType="notice" parameterType="int">
		select * from tbl_notice where notice_id=#{noticeId}
	</select>
</mapper>

